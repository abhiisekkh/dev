git config --global user.email "abba23cs@cmrit.ac.in"

git config --global user.name "abhiisekkh"

ssh-keygen -t ed25519 -C "abba23cs@cmrit.ac.in"

cat ~/.ssh/id_ed25519.pub

ssh -T git@github.com

git init

git add pom.xml

git add src

git commit -m "first commit"

git branch

 git remote add origin git@github.com:abhiisekkh/dev6

git remote set-url origin git@github.com:abhiisekkh/dev6.git

git config --global push.autoSetupRemote true

git push origin master

sudo cat /var/lib/jenkins/secrets/initialAdminPassword

pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                git branch: 'master',
                url: 'https://github.com/abhiisekkh/pgm6.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }

        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }

    }
}



